apiVersion: 1

providers:
  - name: "stellar-stream"
    orgId: 1
    folder: "Stellar Stream"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
