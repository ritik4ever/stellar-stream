Closes #747

Tracking note for issue #747 (add stream QR code so the recipient can scan to open the stream detail). The frontend package.json is empty in this repo, so the feature could not be implemented here; please close the issue if this is being tracked separately.
